<div class="row">
  <div class="col-md-12" ng-controller="Designer.OrganizationsController">
    <h1>Organizations</h1>

    <ul>
      <li ng-repeat="org in organizations">
        <a href="/policy/{{org.name}}">{{org.name}}</a>
        <p>{{org.description}}</p>
      </li>
    </ul>
  </div>
</div>
