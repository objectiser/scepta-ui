/// <reference path="../../includes.ts"/>
/// <reference path="designerGlobals.ts"/>
module Designer {

  export var _module = angular.module(Designer.pluginName, []);

  var tab = undefined;

  _module.config(['$locationProvider', '$routeProvider', 'HawtioNavBuilderProvider', ($locationProvider, $routeProvider:ng.route.IRouteProvider, builder:HawtioMainNav.BuilderFactory) => {
    tab = builder.create()
      .id(Designer.pluginName)
      .title(() => "Policy")
      .href(() => "/policy")
      .subPath("Designer", "page1", builder.join(Designer.templatePath, 'page1.html'))
      .build();
    builder.configureRouting($routeProvider, tab);
    $locationProvider.html5Mode(true);
  }]);

  _module.run(['HawtioNav', (HawtioNav:HawtioMainNav.Registry) => {
    HawtioNav.add(tab);
    log.debug("loaded");
  }]);


  hawtioPluginLoader.addModule(Example.pluginName);
}
